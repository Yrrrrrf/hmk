# Instrucciones de uso

1. Dentro del directorio root (hmk) utilizar docker-compose o podman-compose.
Esto iniciará el contenedor the la base de datos y el servidor que la expone a través de la API
```
docker-compose up --build
podman-compose up --build
```

2. Usar el archivo.war del proyecto con Tomcat! :D